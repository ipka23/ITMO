import java.util.Map;

public class CoordinateValidator {
    public static boolean validate(Map<String, String> coords) {
        return validateX(coords.get("x")) && validateY(coords.get("y")) && validateR(coords.get("r"))
    }

    private static boolean validateX(String x) {
        if
    }

    private static boolean validateR(String y) {
    }

    private static boolean validateY(String r) {
    }

}
